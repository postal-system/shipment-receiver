spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/shipment_store
    username: postgres
    password: mcpmwC8gGwVKr4Q9xCKZEzEgS
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  kafka:
    bootstrap-servers: localhost:9092
    letter-topic: "letter"
    parcel-topic: "parcel"
    portion-topic: "portions"
    consumer:
      auto-offset-reset: earliest
      groupId: "shipment"
  feign:
    person-client:
      name: "personservice"
      url: "http://localhost:8081"
    post-office-client:
      name: "postoffice"
      url: "http://localhost:8084"
  rabbitmq:
    portion-queue: "portionsofshipments"
  active-broker: KAFKA
scheduler:
  fixed-delay: 30000
portion:
  size: 10
server:
  port: 8082